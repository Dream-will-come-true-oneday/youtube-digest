# YouTube Digest 二开需求规格

## 背景

YouTube Digest 已经能够在 Chrome 侧边栏中提供字幕、中文翻译、AI 概览、
选中文本讲解和时间戳笔记。本轮二开需要改善长视频导航和阅读体验，增加字幕
检索与适配视频类型的概览模板，并通过本地生词本和可移植导出形成完整的学习闭环。

部分能力已经实现。本规格统一定义整轮二开的预期行为，用同一组验收标准检查
既有实现和剩余工作。

## 目标

- 让长视频中的定位、跟随播放和回看更加准确。
- 在明亮和昏暗环境中都能舒适阅读字幕。
- 帮助用户快速找到字幕片段，并按视频类型生成适合的概览。
- 让用户把 AI 讲解保存为可复习、可回到原视频位置的生词条目。
- 让用户能用通用格式带走笔记和生词数据。
- 保持用户自备 API Key、本地存储和无遥测的产品模式。

## 功能需求

- F1：所有面向用户的视频时间戳都必须支持超过一小时的视频；播放跟随功能必须
  持续跟踪当前 YouTube 标签页。
- F2：侧边栏必须支持明亮和深色主题以及快捷切换；设置页必须提供明亮、深色、
  跟随系统三种选项，并在本地记住所选偏好。
- F3：用户必须能够搜索当前显示的字幕，看到匹配文本高亮和当前/总匹配数，并能
  通过键盘向前或向后跳转匹配项。
- F4：用户必须能够使用通用、课程、访谈、教程四种模板生成概览。切换模板时必须
  重新生成概览，同时保持既有的概览结果结构。
- F5：用户获取选中字幕的 AI 讲解后，必须能够把词语、讲解、来源视频和播放时间戳
  保存到本地生词本。生词本必须支持当前视频/全部视频筛选、回到时间戳播放、复制
  词语或链接以及删除条目。
- F6：用户必须能够把全部本地笔记或生词导出为 Markdown 或 CSV 文件，并保留视频
  标识、时间戳、链接以及对应的笔记或生词内容。
- F7：设置页必须提供单独删除全部生词的操作；重置全部扩展数据也必须删除生词；
  面向用户的文档必须说明生词的存储、删除、导出和服务商数据流向。
- F8：中英文 README 必须明确标注原仓库与当前二开仓库，使用可快速扫描的对比表
  说明新增功能和体验优化，并删除重复、易过期或不影响安装使用的长篇内容。

## 非功能需求

- N1：API Key 必须由用户提供并仅作为本地设置保存，不得进入源码、测试夹具、
  发布压缩包或文档。
- N2：生词数据必须保存在 Chrome 扩展本地存储中，并且最多保留最新 500 条有效记录。
- N3：来自字幕、视频元数据、用户输入或 AI 响应的数据不得向扩展界面注入可执行标记。
- N4：既有字幕、翻译、概览、讲解、笔记、设置、隐私和发布检查行为不得回归。
- N5：完成后的扩展必须继续兼容文档声明的 Chrome 116 及更高版本，且不需要构建步骤。
- N6：两份 README 必须保持相同的信息结构和事实口径，保留安装、API Key、使用、
  隐私、限制和开发检查所需的最少信息，并通过官方链接承接易变化的价格详情。

## 不做的事

- Anki 导出或与第三方笔记应用直接集成。
- 云同步、账号、分析统计或遥测。
- 新增翻译语言或替换 AI 服务商、模型。
- 未经用户明确保存就自动抽取生词。
- 发布到 Chrome 应用商店。

## 验收标准

- AC1：一小时以内的时间戳显示为 `M:SS`，一小时及以上显示为 `H:MM:SS`；点击
  时间戳能跳到预期位置，且不会造成字幕布局偏移。
- AC2：主题控件能明显切换侧边栏和设置页主题、持久化所选模式；跟随系统模式能
  响应操作系统颜色偏好。
- AC3：字幕搜索能在输入及字幕重新渲染后更新，高亮全部可见匹配项，报告匹配导航
  状态，并支持 Enter 和 Shift+Enter 导航且不改变字幕内容。
- AC4：四种概览模板均可选择，每种成功结果都能渲染章节、重点引用和关键时刻；
  选择不同模板会为该模板发起新的分析。
- AC5：在有效 DeepSeek 配置下，选中文本的讲解可保存一次，并以正确的词语、讲解、
  来源视频和时间戳出现在生词本中，之后能够复制、播放、筛选和删除。
- AC6：笔记和生词导出会下载非空的 `.md` 和 `.csv` 文件；内容含逗号、引号或换行
  时 CSV 仍可解析，导出的链接能打开正确的视频时间点。
- AC7：删除生词不会删除笔记或 API 设置；重置全部数据会一并删除生词；中英文文档
  准确描述已完成功能和本地数据生命周期。
- AC8：自动化测试、JavaScript 语法检查、发布检查和 Chrome 端到端场景全部通过，
  且任何输出都不暴露 API Key。
- AC9：两份 README 均不超过 150 行；首屏可看到项目定位与仓库关系；对比表覆盖
  长视频时间戳、主题、字幕搜索、概览模板、生词本、导出和可访问性优化；安装步骤
  指向当前仓库，同时保留原仓库链接和必要的官方服务商链接。
